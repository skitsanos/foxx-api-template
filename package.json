{
  "name": "foxx-service",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "bundle": "zip -r service.zip --exclude=*.git* --exclude=*.gitignore* --exclude=*.DS_Store* --exclude=*.idea* --exclude=*node_modules* ."
  }
}
